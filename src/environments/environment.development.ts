/** Configuration de développement (remplace `environment.ts` en `ng serve`). */
export const environment = {
  production: false,
  /** Vide = même origine ; le fichier est servi à /api/plats.json. */
  serverUrl: '',
  restaurantName: 'Délices de Douala',
};
